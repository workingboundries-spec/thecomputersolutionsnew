## Goal
Repoint this Lovable project from the current Lovable-managed Supabase backend (`khrdxrztlmndkmnoecyi`) to your own Supabase project (`xmlbknkjhliwnsubgqxk`), with minimal code changes since your schema, RLS, and data are already migrated.

## Important caveat
Lovable Cloud and the "external Supabase Integration" are two different connection modes. Once Lovable Cloud is disabled on this project, it cannot be re-enabled later. After the switch:
- `src/integrations/supabase/client.ts` and `src/integrations/supabase/types.ts` will no longer be auto-regenerated by Lovable Cloud — they become managed via the external Supabase Integration instead.
- The Cloud-managed tools (Cloud → Database UI, Secrets, Edge Functions deploy panel) will switch to operating against your own Supabase project.
- The bootstrap admin user and any data still in the old Lovable-managed DB will not be touched — but the app will stop reading from it.

## Steps

### 1. You disconnect Lovable Cloud and connect your own Supabase (manual, on your side)
This part you must do in the UI — it cannot be done from code:
1. Go to **Connectors** (sidebar) → **Lovable Cloud** → **Disable Cloud** for this project.
2. Then connect the **Supabase Integration** to this project and select your own Supabase project (`xmlbknkjhliwnsubgqxk`).
3. After connecting, Lovable will rewrite `.env`, `src/integrations/supabase/client.ts`, and regenerate `src/integrations/supabase/types.ts` against your new project automatically.

The values that will end up in `.env` should match:
- `VITE_SUPABASE_URL=https://xmlbknkjhliwnsubgqxk.supabase.co`
- `VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_isUTFLRp4HcOiTPkY9sLVQ_qMWwT5l2`
- `VITE_SUPABASE_PROJECT_ID=xmlbknkjhliwnsubgqxk`

### 2. Verify migrated backend pieces exist in your new Supabase project
Confirm in your new project's Supabase dashboard that you have:
- All `crm_*` tables, `crm_user_roles`, `app_role`/`crm_app_role` enums.
- DB functions: `has_crm_role`, `is_crm_user`, `update_updated_at_column`, `assign_crm_catalogue_item_code`, plus the `crm_catalogue_code_seq` sequence.
- Storage buckets: `customer-photos`, `shop-assets` (public), with the same policies.
- All RLS policies enabled on each table.
- An auth user for `admin@thecomputersolutions.in` with a row in `crm_user_roles` granting `crm_admin`.

If anything is missing, fix it in your new Supabase project before going further.

### 3. Re-deploy edge functions to your new Supabase project
The functions `bootstrap-admin`, `crm-import`, and `check_daily_reminders` live in `supabase/functions/`. Once the Supabase Integration is connected, I will:
- Re-deploy these functions against your new project.
- Set the required runtime secrets in your new project (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ANON_KEY`, `LOVABLE_API_KEY` if you still want AI features, etc.). You'll need to confirm/provide the service role key for the new project — the rest can be auto-derived.

### 4. Code adjustments I will make after the switch
After you've completed step 1, I will (in default mode):
- Verify `src/integrations/supabase/client.ts` and `types.ts` were regenerated against the new project. If for any reason they weren't, regenerate `types.ts` and confirm `client.ts` reads from the new env vars.
- Sanity-check that no code hardcodes the old project ref `khrdxrztlmndkmnoecyi` (search and replace if any leftover URLs exist).
- Re-deploy edge functions and confirm CORS + auth still work.
- Run the app and verify: public site loads, `/admin/login` works, `/crm/login` works, CRM pages read/write correctly, image uploads to storage work.

### 5. Verification checklist (what you'll confirm at the end)
- [ ] Public homepage renders the same data (categories, CCTV products, daily deals, etc.).
- [ ] Admin login at `/admin/login` succeeds with `admin@thecomputersolutions.in`.
- [ ] CRM login at `/crm/login` succeeds and the dashboard loads customers/sales/services/enquiries from the new DB.
- [ ] Edge functions respond (test `bootstrap-admin` and `crm-import`).
- [ ] Image uploads land in the new project's storage buckets.

## What I need from you to proceed
1. Do step 1 above (disable Lovable Cloud, connect your Supabase project) — only you can do this from the Connectors UI.
2. After it's connected, reply here so I can run step 3 and 4 (redeploy functions, verify code, run sanity tests).
3. Have your new project's `SUPABASE_SERVICE_ROLE_KEY` ready so I can set it as a runtime secret for the edge functions.

Once you confirm step 1 is done, I'll handle the rest.